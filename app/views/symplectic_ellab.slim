/ ----------------------------------------------
/        app/view/symplectic_ellab.slim
/ Time-stamp: <2016-03-29 00:16:21 (ryosuke)>
/ ----------------------------------------------
/ variables --------------------------
ruby:
  glyph_option = { :class => 'glyphicon glyphicon-exclamation-sign', :aria => {hidden: 'true' } }

/ title ------------------------------
blockquote
  | Calculation of l(a)|b| with symplectic expansion

/ Input form -------------------------
.panel.panel-default
  .panel-heading
    h3.panel-title
      | Input two words of the free group generated by {a,b,s,t,x,y}:
  .panel-body
    form.form-inline action="/Symplectic/ellab" method="post"
      .form-group>
        label> for="word1"
          ' word 1:
        input type="text" name="wa" class="form-control" autocomplete="off"
      .form-group>
        label> for="word2"
          '  word 2:
        input type="text" name="wb" class="form-control" autocomplete="off"
      button type="submit" class="btn btn-default"
        | Submit

/ alert ------------------------------
- if @params[:wa] == 'alert'
  .alert.alert-warning role="alert"
    span{ glyph_option }
      |  You can input the letters 'abstxy' and their capitals only.

/ Output ------------------------------
.bs-docs-example data={ content: "OUTPUT" } = @output11

.bs-docs-example data={ content: "OUTPUT" } = @output12

.bs-docs-example data={ content: "OUTPUT" } = @output2

/ End of file -------------------------
